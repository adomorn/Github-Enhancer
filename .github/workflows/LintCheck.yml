name: Chrome Extension CI

on:
  push:
    branches: [ main, develop, test ]
  pull_request:
    branches: [ main, develop, test ]

permissions:
  contents: write
  pull-requests: write
  issues: write

jobs:
  lint-and-package:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Use Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '16'

    - name: Install ESLint and plugins
      run: |
        npm install eslint@7.32.0 \
                  @typescript-eslint/eslint-plugin@4.33.0 \
                  @typescript-eslint/parser@4.33.0

    - name: Create ESLint config if not exists
      run: |
        if [ ! -f ".eslintrc.js" ] && [ ! -f ".eslintrc.json" ] && [ ! -f ".eslintrc.yaml" ] && [ ! -f ".eslintrc.yml" ]; then
          echo "module.exports = {
            root: true,
            extends: [
              'eslint:recommended',
              'plugin:@typescript-eslint/recommended',
            ],
            parser: '@typescript-eslint/parser',
            plugins: ['@typescript-eslint'],
            rules: {
              '@typescript-eslint/ban-ts-comment': 'off',
              '@typescript-eslint/no-explicit-any': 'off',
              'no-unused-vars': 'off',
              '@typescript-eslint/no-unused-vars': ['warn', { 
                'argsIgnorePattern': '^_', 
                'varsIgnorePattern': '^_',
                'caughtErrorsIgnorePattern': '^_'
              }],
              'no-undef': 'warn'
            },
            env: {
              browser: true,
              node: true,
              es6: true,
              webextensions: true
            },
            parserOptions: {
              ecmaVersion: 2020,
              sourceType: 'module'
            },
            globals: {
              chrome: 'readonly',
              GithubExtensionSettings: 'readonly',
              enhanceRelativeTimes: 'readonly',
              isEnhancing: 'writable',
              enhancementComplete: 'writable',
              enhanceContributors: 'readonly',
              observeDomForElement: 'readonly',
              debug: 'readonly',
              formatDateTime: 'readonly'
            }
          };" > .eslintrc.js
          echo "Created .eslintrc.js"
        else
          echo "ESLint config already exists"
        fi

    - name: Run ESLint
      run: npx eslint . --ext .js,.jsx,.ts,.tsx

    - name: Package extension
      run: |
        zip -r github-enhancer.zip . -x "*.git*" "node_modules/*" "*.md" ".github/*" "package*.json" ".eslintrc.js"

    - name: Upload artifact
      uses: actions/upload-artifact@v3
      with:
        name: github-enhancer-${{ github.ref_name }}
        path: github-enhancer.zip

  create-release:
    needs: lint-and-package
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop' || github.ref == 'refs/heads/test')

    steps:
    - name: Download artifact
      uses: actions/download-artifact@v3
      with:
        name: github-enhancer-${{ github.ref_name }}

    - name: Create Release
      id: create_release
      uses: actions/create-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: ${{ github.ref_name }}-${{ github.sha }}
        release_name: Release ${{ github.ref_name }} ${{ github.sha }}
        draft: false
        prerelease: ${{ github.ref != 'refs/heads/main' }}

    - name: Upload Release Asset
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }}
        asset_path: ./github-enhancer.zip
        asset_name: github-enhancer-${{ github.ref_name }}.zip
        asset_content_type: application/zip
